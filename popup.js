document.addEventListener("DOMContentLoaded", function(event) {
    var resultsButton = document.getElementById("btnSetApiKey");
    resultsButton.onclick = setApiKey();
});

function setApiKey(){ 
    
}